{
  "name": "quiz-platform-backend",
  "version": "1.0.0",
  "description": "Online Quiz Platform Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r build/* ../backend/public/",
    "build:win": "cd ..\\frontend && npm install && npm run build && xcopy build ..\\backend\\public /E /I /Y"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
